"""Pytest configuration and environment fixtures."""

from collections.abc import Generator

import pytest

from {{ package_name }}_env.env.gym_env import CustomEnv


@pytest.fixture
def env() -> Generator[CustomEnv, None, None]:
    """Fixture providing a clean headless environment."""
    test_env = CustomEnv(render_mode=None)
    yield test_env
    test_env.close()
